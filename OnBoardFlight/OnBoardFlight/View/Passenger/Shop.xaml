<Page
    x:Class="OnBoardFlight.View.Passenger.Shop"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OnBoardFlight.View.Passenger"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Height="800">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
            <StackPanel Orientation="Horizontal">
                <ListView ItemsSource="{Binding CategoryListProductList}" x:Name="categoryList">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding Category}"/>
                                <ListView x:Name="productList" ItemsSource="{Binding ProductList}"
                                    ScrollViewer.VerticalScrollBarVisibility="Disabled" 
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.HorizontalScrollMode="Enabled" Tapped="SelectProduct"                  >
                                    <ListView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Background="Transparent" Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ListView.ItemsPanel>
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Vertical" Height="100" Background="DarkGray">
                                                <Image Source="{Binding ImageLink}"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Description}"/>
                                                    <TextBlock Text="€" Margin="30 0 0 0"/>
                                                    <TextBlock Text="{Binding Price}"/>
                                                </StackPanel>
                                            </StackPanel>

                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" Width="600">
                <StackPanel Orientation="Vertical" >
                    <TextBlock Text="Order:"/>
                    <StackPanel Orientation="Horizontal" Width="350">
                        <TextBlock Text="Number" Width="50"/>
                        <TextBlock Text="Description" Width="200"/>
                        <TextBlock Text="TotalPrice" Width="50"/>
                    </StackPanel>
                    <ListView ItemsSource="{Binding Cart.Order.Orderlines}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" BorderBrush="White" BorderThickness="1" Width="350">
                                    <TextBlock Text="{Binding Number}" Width="50"/>
                                    <TextBlock Text="{Binding Description}" Width="200" Margin="20 0 20 0"/>
                                    <TextBlock Text="€"/>
                                    <TextBlock Text="{Binding TotalPrice}" Width="50"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>

