<Page
    x:Class="OnBoardFlight.View.Passenger.Shop"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OnBoardFlight.View.Passenger"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Height="800">
        <StackPanel Orientation="Horizontal">
            <Button Content="Add to cart" Command="{Binding AddToCartCommand}"/>
            <StackPanel Orientation="Horizontal">
            <ListView ItemsSource="{Binding CategoryListProductList}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Category}"/>
                            <ListView x:Name="productList" ItemsSource="{Binding ProductList}"
                                ScrollViewer.VerticalScrollBarVisibility="Disabled" 
                                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                ScrollViewer.HorizontalScrollMode="Enabled"                  >
                                <ListView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Background="Transparent" Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ListView.ItemsPanel>
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical" Height="100" Background="DarkGray">
                                            <Image Source="{Binding ImageLink}"/>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Description}"/>
                                                <TextBlock Text="€" Margin="30 0 0 0"/>
                                                <TextBlock Text="{Binding Price}"/>
                                                <Button Content="Add to cart" Command="{Binding Path= AddToCartCommand}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            </StackPanel>
            <ScrollViewer>
                <StackPanel Orientation="Vertical">
                    <TextBox Text="Order:"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Number"/>
                        <TextBlock Text="Description"/>
                        <TextBlock Text="TotalPrice"/>
                    </StackPanel>
                    <ListView ItemsSource="{Binding Cart.Order.Orderline}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" BorderBrush="White" BorderThickness="1">
                                    <TextBlock Text="{Binding Number}"/>
                                    <TextBlock Text="{Binding Description}"/>
                                    <TextBlock Text="{Binding TotalPrice}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </StackPanel>
    </ScrollViewer>
</Page>

