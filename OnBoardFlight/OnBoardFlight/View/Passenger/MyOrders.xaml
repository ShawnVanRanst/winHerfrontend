<Page
    x:Class="OnBoardFlight.View.Passenger.MyOrders"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OnBoardFlight.View.Passenger"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
        <StackPanel Orientation="Horizontal">
            <TextBox Text="Orders van "/>
            <TextBox Text="{Binding Order.Passenger.Firstname} "/>
            <TextBox Text="{Binding Passenger.Firstname}"/>
        </StackPanel>
        
        <ListView ItemsSource="{Binding Order.Orderlines}" x:Name="OrderlinesListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Path=Number}" x:Name="number"/>
                        <TextBlock Text="{Binding Path=Product.Name}" x:Name="productname"/>
                        <TextBlock Text="{Binding Path=TotalPrice}" x:Name="price"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>    
</Page>
